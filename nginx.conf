server {
    listen 80;
    listen [::]:80;
    server_name _;

    # Увеличиваем размер буфера для headers
    large_client_header_buffers 4 32k;

    # Корневая директория приложения
    root /app/dist;
    index index.html index.htm;

    # Gzip компрессия
    gzip on;
    gzip_min_length 1000;
    gzip_types text/plain text/css text/javascript application/javascript application/json;

    # Обслуживание статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # SPA routing: все запросы перенаправляются на index.html
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Отключаем обслуживание скрытых файлов
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Логирование
    access_log /var/log/nginx/access.log combined;
    error_log /var/log/nginx/error.log warn;
}

# HTTPS конфигурация (раскомментируйте для использования с Let's Encrypt)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#
#     # Остальная конфигурация как выше...
# }
#
# # Редирект с HTTP на HTTPS
# server {
#     listen 80;
#     listen [::]:80;
#     server_name your-domain.com;
#     return 301 https://$server_name$request_uri;
# }
